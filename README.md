# Project fetalskin_regionalid 

[github repo] (git@github.com:haniffalab/fetalskin_regionalid.git)

# New users
```
git init
git remote add origin git@github.com:haniffalab/fetalskin_regionalid.git
git branch -M main
git push -u origin main
```
